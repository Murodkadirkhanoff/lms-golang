# Bu faylni `.env` ga nusxalab, qiymatlarni to'ldiring.
# .env repoga TUSHMAYDI (.gitignore'da).

POSTGRES_USER=lms
POSTGRES_PASSWORD=change_me_in_real_env
POSTGRES_DB=lms

# Go ilovasi uchun ulanish satri (cmd/api -db-dsn flag yoki LMS_DB_DSN env):
# postgres://lms:change_me_in_real_env@localhost:5432/lms?sslmode=disable
